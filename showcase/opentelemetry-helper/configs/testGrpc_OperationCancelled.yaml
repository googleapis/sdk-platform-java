receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4320"

exporters:
  file:
    path: "testGrpc_OperationCancelled_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]