receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4319"

exporters:
  file:
    path: "testGrpc_attemptFailedRetriesExhausted_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]