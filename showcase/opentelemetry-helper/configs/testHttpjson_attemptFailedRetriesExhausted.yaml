receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4324"

exporters:
  file:
    path: "testHttpjson_attemptFailedRetriesExhausted_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]