receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4325"

exporters:
  file:
    path: "testHttpjson_attemptPermanentFailure_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]