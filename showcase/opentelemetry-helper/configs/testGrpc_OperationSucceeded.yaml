receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4318"

exporters:
  file:
    path: "testGrpc_OperationSucceeded_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]