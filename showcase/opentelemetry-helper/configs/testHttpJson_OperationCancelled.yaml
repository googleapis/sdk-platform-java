receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4319"

exporters:
  file:
    path: "testHttpJson_OperationCancelled_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]