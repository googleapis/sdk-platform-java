receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4326"

exporters:
  file:
    path: "testGrpc_attemptPermanentFailure_metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]