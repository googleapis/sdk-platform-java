receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:4317"

exporters:
  file:
    path: "./metrics.txt"

service:
  extensions: []
  pipelines:
    metrics:
      receivers: [otlp]
      processors: []
      exporters: [file]