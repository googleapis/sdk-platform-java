package com.google.cloud.spanner;

import javax.annotation.Generated;

@Generated("by gapic-generator-java")
class AutoGeneratedOptions {
  private String requestTag;
  private Integer maxBatchingDelay;

  public static RequestTag requestTag(String requestTag) {
    return new RequestTag(requestTag);
  }

  public static MaxBatchingDelay maxBatchingDelay(Integer maxBatchingDelay) {
    return new MaxBatchingDelay(maxBatchingDelay);
  }

  protected boolean hasRequestTag() {
    return requestTag != null;
  }

  protected boolean hasMaxBatchingDelay() {
    return maxBatchingDelay != null;
  }

  protected String requestTag() {
    return requestTag;
  }

  protected Integer maxBatchingDelay() {
    return maxBatchingDelay;
  }

  protected static final class RequestTag extends InternalOption implements Options {
    private final String requestTag;

    public RequestTag(String requestTag) {
      this.requestTag = requestTag;
    }

    @Override
    public void appendToOptions(Options options) {
      options.requestTag = requestTag;
    }
  }

  protected static final class MaxBatchingDelay extends InternalOption
      implements TransactionOption {
    private final Integer maxBatchingDelay;

    public MaxBatchingDelay(Integer maxBatchingDelay) {
      this.maxBatchingDelay = maxBatchingDelay;
    }

    @Override
    public void appendToOptions(Options options) {
      options.maxBatchingDelay = maxBatchingDelay;
    }
  }
}
